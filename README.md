# Montage Manager

Go + htmx app for `mm.machinemachine.ai`.

## Run

```sh
docker build -t mm-machine .
docker run --rm -p 8080:8080 mm-machine
```

Open `http://localhost:8080`.

Production: `https://mm.m-2.cc`.
